User:
  dbs: [mysql, redis]
  dbname: ezorm
  dbtable: users
  fields:
    - Id: int32
      flags: [primary, range]
    - Name: string
    - Mailbox: string
      comment: 邮箱
    - Sex: bool
      flags: [index]
    - Age: int32
      flags: [range, order, nullable]
    - Longitude: float64
    - Latitude: float64
    - Description: string
      flags: [nullable]
    - Password: string
    - HeadUrl: string
      flags: [nullable]
    - Status: int32
    - CreatedAt: timeint
    - UpdatedAt: timeint
    - DeletedAt: timeint
      flags: [nullable]
  uniques: [[Mailbox, Password]]
  importSQL: "SELECT `id`,`name`,`mailbox`,`sex`, `age`, `longitude`,`latitude`,`description`,`password`,`head_url`,`status`,`created_at`, `updated_at`, `deleted_at` FROM users"

UserBaseInfo:
  dbs: [mysql]
  dbname: ezorm
  dbview: user_base_info
  fields:
    - Id: int32
      flags: [primary]
    - Name: string
      flags: [index]
      size: 20
    - Mailbox: string
    - Password: string
    - Sex: bool
  uniques: [[Mailbox, Password]]
  importSQL: "SELECT `id`,`name`,`mailbox`,`sex` FROM users"

UserLocation:
  dbs: [redis, mysql]
  relation:
    storetype: geo
    valuetype: int32
    modeltype: User
  importSQL: "SELECT 'all',`longitude`,`latitude`,`id` FROM users"  

SexUserLocation:
  dbs: [redis, mysql]
  relation:
    storetype: geo
    valuetype: int32
    modeltype: User
  importSQL: "SELECT `sex`,`longitude`,`latitude`,`id` FROM users"  

UserId:
  dbs: [redis, mysql]
  relation:
    storetype: list
    valuetype: int32
    modeltype: User
